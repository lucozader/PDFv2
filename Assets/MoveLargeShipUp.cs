using UnityEngine;
using System.Collections;

public class MoveLargeShipUp : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		Vector3 victor = new Vector3(transform.position.x,transform.position.y+300f,transform.position.z);
	
	}
}
